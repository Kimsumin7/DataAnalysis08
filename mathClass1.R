# 수학점수 1반 데이터
math_scores_1 <- c(40, 60, 100, 90, 70, 60, 90, 60, 70, 60)

# 평균 계산
mean_1 <- mean(math_scores_1)
mean_1

# 분산 계산
variance_1 <- var(math_scores_1)
variance_1

# 표준편차 계산
sd_1 <- sd(math_scores_1)
sd_1
