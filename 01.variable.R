# 변수 x에 값 3을 할당합니다
x <- 3

# 변수 x의 값을 출력합니다. 결과는 3입니다.
x

# 변수 x의 값을 5로 변경합니다
x <- 5

# 변수 x의 값을 출력합니다. 결과는 5입니다.
x

# 변수 x를 NULL로 초기화합니다
x <- NULL

# 변수 y에 값 3을 할당합니다
y <- 3

# 변수 y의 값을 temp에 저장합니다
temp <- y

# 변수 temp의 값을 출력합니다. 결과는 3입니다.
temp

# 변수 y의 값을 출력합니다. 결과는 3입니다.
y

# 변수 y의 값을 x의 값으로 변경합니다. 현재 x는 NULL입니다.
y <- x

# 변수 y의 값을 출력합니다. 결과는 NULL입니다.
y

# 변수 x의 값을 출력합니다. 결과는 NULL입니다.
x

# 변수 x의 값을 temp의 값으로 변경합니다. temp의 값은 3입니다.
x <- temp

# 변수 x의 값을 출력합니다. 결과는 3입니다.
x

# 변수 y의 값을 출력합니다. 결과는 NULL입니다.
y
